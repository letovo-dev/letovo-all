name: Mac CI request

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  complete:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "Trusted controller will verify this frozen pull request."
